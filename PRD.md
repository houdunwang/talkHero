# TalkHero 产品需求文档

## 1. 产品定义

TalkHero 是一款本地 AI 视频对口型桌面软件。用户从一段 B 视频创建并保存可复用的音色档案，输入独立文案生成该音色的新音频，再让 A 视频中的指定人物只改变嘴部口型以匹配音频。生成完成后，软件可以在本地生成标题、简介、话题与视频封面，并在用户明确触发后自动打开抖音或小红书发布页面、填写表单、上传素材和提交发布。

产品优先保护原视频：除嘴部及自然融合所必需的最小邻域外，不主动处理人物、动作、背景或其他画面内容。

## 2. 产品目标

- 无需按次付费服务，核心音色、语音、口型、封面和文案能力在用户本机完成。
- B 视频只在创建或更新音色时使用一次，音色档案可被后续多个视频重复使用。
- 支持人物坐着、站立或正常走动的 A 视频，在面部持续清晰可见时生成自然、同步的嘴部口型。
- 默认兼顾消费级 NVIDIA 显卡的生成质量与速度，并为高配置设备预留高清口型模式。
- 将视频生成和平台发布分离；用户可以只导出视频，也可以明确触发一键发布。

## 3. 非目标

首版不提供以下能力：

- 换脸、全脸重绘、数字人身体或动作生成。
- 美颜、调色、背景替换、智能运镜、补帧或通用视频剪辑。
- 多人同时说话或自动判断多个说话人的复杂时间线。
- 绕过验证码、扫码、平台风控、审核或用户确认。
- 在后台静默发布内容。
- 依赖必须按量付费的云端音色、口型或内容生成 API。
- 承诺任意低质量、严重侧脸、遮挡或运动模糊素材均能自然生成。

## 4. 目标平台与硬件

### 4.1 首版目标平台

- Windows 11 x64。
- NVIDIA CUDA 显卡。
- 完整 GPU 推理首先在 Windows NVIDIA 环境交付和验证。

Electron 桌面壳继续兼容 macOS 15+ Apple Silicon，但在对应模型与推理链路完成真实验证之前，不宣称其生成速度、质量或完整能力与 Windows 等价。

### 4.2 显卡档位

- 4GB 显存：兼容模式，模型必须分时加载、小批量运行，允许明显更长的生成时间。
- 6GB 显存：首版最低建议配置。
- 8GB～12GB 显存：默认推荐配置。
- 18GB 及以上：可承载后续 LatentSync 1.6 高清模式。

### 4.3 性能目标

以模型已安装并完成热启动、单人物、1 分钟、1080p、25/30fps、面部清晰的合格素材为基准：

- RTX 3060 Laptop 6GB：目标在 8 分钟内完成 IndexTTS 2.5 音频与 MuseTalk 1.5 视频生成。
- 同一 A 视频重复生成时复用人脸跟踪缓存，目标进一步缩短生成时间。
- 4GB 显存只承诺兼容性，不将几分钟内完成作为首版硬指标。

以上为产品目标，必须由真实硬件基准验证；未验证前不得作为已实现性能对外宣传。

## 5. 核心概念

### 5.1 A 视频

需要改变人物嘴部口型的源视频：

- 时长不超过 3 分钟。
- 支持坐姿、站立和正常走动。
- 默认单个主要人物。
- 人脸和嘴部需要在目标说话时段内清晰可见。
- 输出默认保持 A 视频的画面比例、分辨率、帧率和时长；因局部编辑必须重新编码，不承诺文件二进制不变。

### 5.2 B 视频

创建音色档案的参考视频：

- 允许 10～60 秒，推荐 15～30 秒，最佳目标约 20 秒。
- 只包含一个目标说话人，且人声清晰、连续、无背景音乐、其他人声、明显回声或爆音。
- B 视频内容不会成为最终文案，也不会参与最终画面。
- 软件自动提取音轨、筛选有效语音、降噪并转写参考文字；用户无需手工提供 B 视频字幕。

### 5.3 音色档案

音色档案是可复用的本地资源，封装：

- 清理后的参考音频。
- 自动识别出的参考文本。
- 模型推理所需的音色特征与元数据。
- 名称、创建时间、试听样本和模型版本。

用户可以创建、试听、重命名、更新、删除、导入和导出音色档案。删除音色时必须删除其受管参考音频和派生特征。音色档案默认只保存在本机。

## 6. 核心用户流程

### 6.1 创建音色

1. 用户上传 B 视频。
2. 软件校验时长、音轨和单说话人质量。
3. 软件提取、清理并自动转写参考人声。
4. 软件创建音色档案并生成短句试听。
5. 用户确认后保存音色；不满意时可以更换参考素材重新创建。

### 6.2 生成音频

1. 用户选择已保存的音色。
2. 用户输入独立文案。
3. 软件估算语音时长，并与 A 视频可用时长比较。
4. 用户选择自然、热情、沉稳、讲解等语气及允许的语速范围。
5. 软件按语义将长文案分段生成并自然拼接。
6. 用户可以先试听音频；确认后才进入耗时更高的口型生成。

### 6.3 生成对口型视频

1. 用户上传 A 视频并选择目标人物。
2. 软件执行人脸清晰度、大小、遮挡、角度、出画和运动模糊质检。
3. 软件跟踪目标人物，内部裁剪和对齐人脸区域。
4. MuseTalk 1.5 使用生成音频驱动局部嘴部口型。
5. 软件通过时序平滑和羽化蒙版，将嘴部及必要的最小融合区域贴回原始画面。
6. FFmpeg 将新音频与视频合成并输出。
7. 软件标记无法可靠生成的时间段，并禁止将明显错误静默当成成功结果。

### 6.4 发布视频

1. 用户选择已生成的视频。
2. 软件根据文案生成标题、简介和相关话题。
3. 软件从视频抽取清晰、构图合适的候选帧，避让人物后套用本地模板生成封面。
4. 用户预览并可修改视频、封面和表单内容。
5. 用户明确点击某个平台的“一键发布”。
6. 软件打开对应创作者发布页面，自动上传视频和封面、填写表单并提交。
7. 遇到登录、扫码、验证码或风控时暂停，等待用户完成后继续。
8. 只有页面返回可验证成功状态时显示提交成功；平台审核结果与提交成功必须区分。

## 7. 功能需求

### 7.1 音色与语音

- 首版默认使用 IndexTTS 2.5。
- 同一个音色档案可服务任意数量的 A 视频和新文案。
- B 视频仅用于音色参考，最终声音内容必须严格来自用户文案。
- 用户可以控制语气和合理范围内的语速，但不得为了强行适配时长生成明显不自然的语音。
- 三分钟文案必须按语义分段生成，统一音色和响度，并处理停顿与段间衔接。
- 音频生成失败时不得启动口型任务；用户可保存和复用已确认音频。

### 7.2 局部口型

- 首版默认使用 MuseTalk 1.5 的 256×256 人脸局部推理。
- 256×256 是内部模型工作区域，不改变最终视频分辨率或画幅。
- 模型可以利用鼻部、脸颊与下巴上下文，但最终只融合嘴部及自然过渡所需的最小邻域。
- 不使用全脸修复或整帧生成来掩盖失败。
- 人脸框、关键点和蒙版必须跨帧平滑，避免嘴部漂移或贴片感。
- 走动场景必须锁定人物身份；出现其他人时不得切换目标。
- 遮挡、严重侧脸、背脸、过小人脸和强运动模糊属于质量风险，软件必须预检并反馈。
- 后续可以增加 LatentSync 1.6 512×512 高清模式，但不得成为首版默认依赖。

### 7.3 原视频保护

- 默认不裁切、不调色、不美颜、不换脸、不补帧、不修改背景、身体、动作和其他脸部特征。
- 人脸检测使用低分辨率代理帧时，合成必须回到原始视频坐标和画面。
- A 视频原有环境声可以由用户选择保留并与新语音混合，或者完全替换；不得未经选择丢弃有声素材。
- 平台比例、字幕或其他发布加工只能生成单独副本，不得覆盖原始比例版本。

### 7.4 封面与发布文案

- 封面在本地生成，是独立图片，不修改视频内容。
- 默认生成 3 张候选封面并自动推荐评分最高的一张。
- 封面标题需要自适应长度、避让人脸并符合竖屏安全区域。
- 标题、简介和话题以用户文案为依据，允许编辑后发布。
- 不生成与内容无关的话题或强导流文本。

### 7.5 平台发布自动化

- 首版支持抖音和小红书，并为两个平台提供独立适配器。
- 每个平台拥有独立页面适配器，不共享脆弱的页面选择器。
- 使用应用管理的独立浏览器资料目录保存登录状态，不读取用户日常 Chrome 配置。
- 自动化只访问配置的官方平台域名，并校验待上传文件来自受管输出或用户明确选择。
- 一键发布必须由用户对当前视频明确触发，自动点击提交属于该次明确操作的一部分。
- 不绕过验证码、扫码、风控与平台审核。
- 页面结构变化、上传失败或状态不确定时停止并保存视频、封面和表单草稿，避免重复发布。

## 8. 模块架构

新增四个稳定模块：

### 8.1 `apps/inference`

- 管理本地 Python Worker 生命周期和有版本的结构化协议。
- 管理模型下载、完整性校验、版本、磁盘占用和删除。
- 检测 GPU、CUDA、显存与可用推理模式。
- 提供任务队列、进度、取消、超时、崩溃恢复和资源释放。
- 管理 IndexTTS、MuseTalk、ASR 与 FFmpeg 适配器，不包含具体产品流程。

### 8.2 `apps/voice`

- 管理 B 视频预处理、参考转写和音色档案生命周期。
- 根据音色、文案、语气和语速生成可试听音频。
- 负责音色和音频持久化契约，不直接处理 A 视频。

### 8.3 `apps/video`

- 提供主要生成工作台和生成任务状态。
- 管理 A 视频探测、质检、人物选择、跟踪、口型、融合和输出。
- 只消费已经确认的音频，不理解 B 视频或平台网页。

### 8.4 `apps/publish`

- 管理标题、简介、话题与封面生成。
- 管理独立浏览器资料目录和平台登录状态。
- 通过 `platforms/douyin`、`platforms/xiaohongshu` 适配网页发布。
- 只消费已生成的视频文件，不参与音色和口型推理。

依赖方向保持为：

```text
apps/core
    ↑
apps/inference
    ↑
apps/voice ──→ apps/video ──→ apps/publish
```

renderer 只能通过最小 preload bridge 发起操作。文件路径、模型配置、发布 URL 和网页状态均由 main 校验；视频和音频内容不得通过 IPC Buffer 传输。

## 9. 技术选型

- 桌面端：Electron 39、React 19、TypeScript、TanStack Router、Tailwind CSS。
- 本地推理：独立 Python Worker、PyTorch/CUDA，业务代码不直接依赖模型仓库内部结构。
- 默认音色与语音：IndexTTS 2.5。
- 默认对口型：MuseTalk 1.5，256×256 局部人脸推理。
- 后续高清口型：LatentSync 1.6，512×512 局部人脸推理。
- 音视频探测、拆分、混音和编码：FFmpeg；支持时优先使用 NVDEC/NVENC。
- 发布自动化：受控 Playwright 浏览器会话或等价的可验证自动化适配器。
- 封面：视频抽帧与本地 HTML/Canvas 模板渲染。
- 标题与简介：本地规则或本地文本模型，不依赖收费 API。

## 10. 性能设计

- IndexTTS 与 MuseTalk 在低显存设备上串行运行，阶段结束后释放显存。
- 人脸检测与跟踪结果按 A 视频内容指纹缓存；同一视频再次生成时复用。
- 使用内存帧管线，避免将全部中间帧以图片落盘后再读取。
- 使用 FP16/BF16、受控批量推理和可验证的 TensorRT/ONNX 优化。
- 使用硬件视频解码与编码，失败时明确降级而非静默改变质量。
- 长视频分段处理并保存检查点，失败后只重试失败片段。
- 速度优化不得扩大嘴部修改区域、降低最终输出规格或跳过质量门禁。

## 11. 质量与验收原则

### 11.1 合格素材

合格素材至少满足：目标人物稳定可识别、目标时段面部清晰、嘴部无遮挡、没有持续严重侧脸或背脸、人物没有小到无法保留嘴部细节。

### 11.2 验收维度

- 音色相似度：由固定测试集的盲听与机器指标共同评估。
- 文案正确性：生成语音不得漏读、错读、重复或添加文案外内容。
- 音画同步：使用同步置信指标并辅以真实视频人工验收。
- 身份保持：除嘴部最小融合区外，不应出现可感知的人脸或画面变化。
- 时序稳定：走动、轻微转头和镜头运动中不得出现明显嘴部漂移、闪烁或贴片边缘。
- 可恢复性：取消、崩溃、断电或显存不足不得破坏原始素材和已完成音色档案。
- 发布准确性：表单内容、封面和视频必须对应当前任务；不确定状态不得报告成功或自动重复提交。

“自然”属于有素材前提的质量目标，不作为对任意输入的无条件保证。首版必须建立覆盖坐姿、正常走动、轻微侧脸、短暂遮挡、远近变化和场景切换的本地验收素材集。

## 12. 隐私、授权与合规

- 创建音色前提示用户确认已取得目标人物对人脸与人声编辑的单独授权。
- 默认不上传 A/B 视频、音色档案、文案和生成结果。
- 日志不得记录完整文案、参考音频、人物视频、浏览器凭据或可还原的生物特征。
- 生成内容应支持依法添加显式 AI 标识与文件元数据隐式标识。
- 发布时使用平台提供的 AI 内容声明能力。
- 模型代码、权重、训练数据和重分发条款必须在发布前逐项完成许可证审查；“本地免费运行”不等于可以忽略许可证或人物授权。

## 13. 首版交付范围

首版 MVP 包含：

- Windows NVIDIA 环境检测和本地模型管理。
- B 视频创建、试听、保存和删除音色档案。
- 选择音色并根据独立文案生成可试听音频。
- A 视频质检、单人物选择和坐姿/正常走动跟踪。
- MuseTalk 1.5 局部嘴部生成与原规格视频输出。
- 任务进度、取消、失败片段报告与有限重试。
- 本地生成标题、简介、话题和 3 张候选封面。
- 抖音和小红书的独立浏览器登录、自动上传、填表和用户触发的一键提交。

后续版本候选：

- LatentSync 1.6 高清模式。
- macOS Apple Silicon 本地推理适配与基准。
- 音色档案跨设备导入导出。
- 多任务队列和批量视频生成。

候选能力不属于首版承诺，进入实现前必须由新的 Spec 明确范围。
