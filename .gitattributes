# 强制所有文本文件在提交到仓库时转换为 LF，并在检出时根据系统自动处理（或保持 LF）
* text=auto eol=lf

# 常见的二进制文件，不进行换行符转换
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.mov binary
*.mp4 binary
*.pdf binary
*.zip binary
*.7z binary
*.gz binary
*.rar binary

# 明确某些文件始终保持 LF (例如 shell 脚本)
*.sh text eol=lf
