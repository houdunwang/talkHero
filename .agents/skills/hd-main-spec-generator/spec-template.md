<!-- managed-by: hd-sdd-tdd-setup -->
# 后盾云 Main 模式 Spec 模板

只在 clean main 的阶段 A 读取。初次状态必须待确认。按需保留章节；不适用内容合并写“无/不涉及”，不重复同一事实。编号为 `FR/EDGE/NFR/AP/AC/Q-001`，每个 AC 至少关联 FR、EDGE 或 NFR。

```markdown
# <功能名称> Spec

## 0. 文档状态
- 状态：待确认 / 已确认；审核结论：未审核 / 已确认
- feature-slug：；项目根目录：；基线分支：main；基线提交：<完整 SHA>
- feature 分支：无（main 模式禁止创建）；worktree：<项目根目录>（现有 main 工作区）
- 初始 Git 状态：clean；最近确认时间：无 / <time>；上游 PRD：无
- 状态说明：

只有用户在上述 main 工作区输入精确 `SPEC审核通过` 后才能确认；实质内容变化必须退回待确认。

## 1. 目标、范围与模块
- 问题、目标用户、完成定义：
- 范围内 / 范围外 / 禁止修改：
- 目标 `apps/<module>` 或业务域及相邻边界：

## 2. 当前与目标行为
- 当前行为、已知限制、可复用能力：
- 主流程：前置条件 → 操作 → 状态变化 → 可观察结果
- 关键失败与恢复：

## 3. 需求与影响
### 功能需求（FR-*）
### 边界与异常（EDGE-*）
### 非功能需求（NFR-*）
- 数据/状态：输入输出、持久化、失效、兼容与恢复：
- 系统影响：renderer/main/preload-IPC/config/文件网络/权限/构建：
- 平台：macOS 15+ Apple Silicon；Windows 11 x64；Linux（仅明确不支持时说明）：

## 4. 范围外变化审批（AP-*）
| 编号 | 类型 | 路径/包/服务 | 必要性与替代 | 影响 | 状态 |
|---|---|---|---|---|---|
无则写“无”；未批准不得进入 Plan。

## 5. 风险与验收
| 风险 | 等级 | 对应编号 | 处理或验证 |
|---|---:|---|---|

### 验收标准（AC-*）
#### AC-001：<场景>
- 关联需求：；前置条件：；操作：；可观察预期：

| AC | 风险 | 验证方式/TDD | 环境或证据 |
|---|---|---|---|

## 6. 发布、恢复与待确认问题
- 现有用户/数据影响、不可逆变化、失败恢复：
| 编号 | 问题 | 状态 | 关闭条件 |
|---|---|---|---|

## 7. Plan 前校验
- [ ] 状态和审核结论已确认；无关键 Q；实施 AP 已批准
- [ ] AC 可追溯，高风险 AC 有验证，范围和平台信息足够实施
```
