export const coreConfig = {
  // 开机自动启动
  autoStart: false,
  // 静默启动
  silentStart: false,
  // 隐藏dock图标
  hideDockIcon: false,
  // 启动时自动检查更新
  autoCheckUpdate: true,
  // 设备安装标识
  installId: ''
}

export type CoreConfigType = typeof coreConfig
